const app = require("../http/bootstrap/app");
const configuration = require("../config");
app.listen(configuration.PORT, () => {
  console.log("Server is up and running on port number " + configuration.PORT);
});
